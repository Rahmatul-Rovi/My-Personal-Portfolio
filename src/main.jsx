import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './App.jsx' // Ba RouterProvider
import './index.css' // Eita thik moto import thaka lagbe

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
)